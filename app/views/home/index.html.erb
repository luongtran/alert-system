<div class="headerMid">
  <div class="container">
    <div class="headerMid-inner clearfix">
      <div class="headerCont1">
        <div class="pull-left">
          <div id="notepad">
            <div class="noteText">
              <% if !@homepage_content.nil? %>
                  <%= raw @homepage_content.notepad_notetext %>
              <% else %>
                  <p>Dear everyone,</p>

                  <p>
                    Bad things happen. Sometimes, they happen to you. If something does happen,
                    you might wish there was something you had told the people around you.
                    How you feel, what you regret, where the money is stashed.<br/>
                    For this, you need this site to keep things most important words wnat to say with your familiers
                    when you can not.

                  </p>
              <% end %>


            </div>
          </div>
        </div>
        <div class="intro">
          <div id="introduce">
            <h3 class="lead">
              <% if !@homepage_content.nil? %>
                  <%= raw @homepage_content.introduce_lead %>
              <% else %>
                  Start your Plan and <br/>
                  keep best stuffs every day!
              <% end %>
            </h3>
            <ul class="unstyled">
              <li><i class="icon-info-sign pull-left"></i>

                <p class="pll">
                  <% if !@homepage_content.nil? %>
                      <%= raw @homepage_content.introduce_inf_1 %>
                  <% else %>
                      Bad things happen. Sometimes, they happen to you. If something does happen, you might wish there
                      was
                      something you had told the people around you.
                  <% end %>

                </p>
              </li>
              <li><i class="icon-info-sign pull-left"></i>

                <p class="pll">
                  <% if !@homepage_content.nil? %>
                      <%= raw @homepage_content.introduce_inf_2 %>
                  <% else %>
                      You write a few e-mails and choose the recipients. These emails are stored securely, so you can be
                      sure that no-one except the intended recipient will ever read them.
                  <% end %>


                </p>
              </li>
              <li><i class="icon-info-sign pull-left"></i>

                <p class="pll">
                  <% if !@homepage_content.nil? %>
                      <%= raw @homepage_content.introduce_inf_3 %>
                  <% else %>
                      The emails are sent at certain intervals. By default, the switch will email you 30, 45, and 52
                      days
                      after you last showed signs of life.
                  <% end %>


                </p>
              </li>
            </ul>
          </div>
        </div>
      </div>
      <div class="rightMid">
        <div id="loginQuickPanel">
          <div class="login-inner">
            <div class="login-inner2">
              <div class="loginHead">
                <span class="title mrm">Login Panel</span><a href="<%= pricing_path %>" class="signupLink"><i class="icon-chevron-right mrm"></i>Sign
                Up</a></div>
              <div class="loginBody">
                <%= form_for "user", :as => :user, :url => session_path(:user) do |f| -%>
                    <fieldset>
                      <div class="control-group">
                        <%= f.text_field :email, :placeholder => "Your Email", :class => "input-block-level", :size => 25 %>
                        <!--<input type="text" name="email" placeholder="Your Email" class="input-block-level handwrite"/>-->
                      </div>
                      <div class="control-group">
                        <%= f.password_field :password, :placeholder => "Your Password", :class => "input-block-level", :size => 25 %>
                        <!--<input type="password" name="password" placeholder="Your Password" class="input-block-level handwrite"/>-->
                        <br/><a href="<%= new_password_path(resource_name) %>" class="forgot">I
                        forgot my password</a>
                      </div>
                      <div class="control-group submitdiv">

                        <button type="submit" class="btn btn-primary"><i class="icon-leaf mrm"></i>Login Now</button>

                        <a href="<%= pricing_path %>" class="btn btn-primary signupsmall">Signup<i class="icon-pencil mlm"></i></a>

                        <div class="rememberme">
                          <label class="checkbox">
                            <%= f.check_box :remember_me, :as => :boolean if devise_mapping.rememberable? %>Remember me
                          </label>
                        </div>
                      </div>
                    </fieldset>
                <% end -%>

              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="pageBody">
  <div class="container">
    <div class="whitebox wizard">
      <div class="row-fluid">
        <div class="span4">
          <div id="signupQuick">
            <div class="signupQuick-inner">
              <% if !@homepage_content.nil? %>
                  <p class="lead"><%= raw @homepage_content.signupquick_lead %></p>
              <% else %>
                  <p class="lead"> Easy to sign up! Choose a plan bellow, fill out the form and ...</p>
              <% end %>

              <table class="table table-hover">
                <tr>
                  <th></th>
                  <th>Amount</th>
                  <th>Packages</th>
                  <th>Encryption</th>
                </tr>
                <tr class="infor">
                  <td><a href="<%= new_user_registration_path(:plan => 'free') %> "><b>Free</b></a></td>
                  <td>$0</td>
                  <td>1</td>
                  <td>Standard</td>
                </tr>
                <tr class="success">
                  <td><a href="<%= new_user_registration_path(:plan => 'small') %> "> <b>Small</b></a></td>
                  <td>$5</td>
                  <td>3</td>
                  <td>Standard</td>
                </tr>
                <tr class="warning">
                  <td><a href="<%= new_user_registration_path(:plan => 'medium') %> "><b>Medium</b></a></td>
                  <td>$10</td>
                  <td>10</td>
                  <td>Standard</td>
                </tr>
                <tr class="error">
                  <td><a href="<%= new_user_registration_path(:plan => 'large') %> "><b>Large</b></a></td>
                  <td>$15</td>
                  <td>30</td>
                  <td>Custom key</td>
                </tr>

              </table>

              <!--<a href="<%= new_user_registration_path(:plan => 'free') %>" class="btn btn-primary">-->
              <!--Free $0/month</a>-->
              <!--<a href="<%= new_user_registration_path(:plan => 'small') %>" class="btn btn-primary">-->
              <!--Small ($5/month)</a>-->
              <!--<br />-->
              <!--<a href="<%= new_user_registration_path(:plan => 'medium') %>" class="btn btn-primary">-->
              <!--Medium ($10/month)</a>-->
              <!--<a href="<%= new_user_registration_path(:plan => 'large') %>" class="btn btn-primary">-->
              <!--Large ($15/month)</a>-->
            </div>
          </div>
          <div class="notice">
            <hr class="divider">
            <p>
              <% if !@homepage_content.nil? %>
                  <%= raw @homepage_content.notice %>
              <% else %>
                  Notice: this is just to quick way to show you out system work. For more feautures please go to
                  DashBoard
                  page after you login with account!
              <% end %>
            </p>
          </div>
        </div>
        <div class="span5">
          <div class="halfpage">
            <div class="halfpage-inner">
              <% if !@homepage_content.nil? %>
                  <h4 class="lead"><%= raw @homepage_content.halfpage_lead %></h4>
              <% else %>
                  <h4 class="lead">Taking a tour to find out <br/> How the AlertSystem Work!</h4>
              <% end %>


              <hr class="divider">
              <ul class="unstyled">
                <li class="clearfix"><a href="#"><i class="icon-chevron-right mrm"></i>Step 1 : Create Account</a></li>
                <li class="clearfix">
                  <a href="#"><i class="icon-chevron-right mrm"></i>Step 2 : Setup setting account</a></li>
                <li class="clearfix">
                  <a href="#"><i class="icon-chevron-right mrm"></i>Step 3 : Add package and recipients</a></li>
                <li class="clearfix">
                  <a href="#"><i class="icon-chevron-right mrm"></i>Step 4 : See your package like recipient</a></li>
              </ul>
            </div>
          </div>
        </div>
        <div class="span3">
          <div class="contentHalfPage">
            <p>
              <% if !@homepage_content.nil? %>
                  <%= raw @homepage_content.halfpage_content %>
              <% else %>
                  Please use your name one you like best. This name will be a default name show on every newsletters -
                  notification email we send to you, or package notifs email to your familiers you assigns
              <% end %>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="pagefooter">
  <div id="tipLarge" class="container">
    <div class="tipUnit">
      <div class="tipUnit-inner">
        <%= image_tag "../assets/images/Bulb.png", :class => "pull-left" %>
        <div class="infoUnit" id="inforUnit1">

          <% if !@homepage_content.nil? %>
              <h3><%= raw @homepage_content.pagefoot_inforunit1_head %></h3>
          <% else %>
              <h3>Everything started from Idea</h3>
          <% end %>


          <p>
            <% if !@homepage_content.nil? %>
                <%= raw @homepage_content.pagefoot_inforunit1_content %>
            <% else %>
                Bad things happen. Sometimes, they happen to you. If something does happen, you might wish there was
                something you had told the people around you. How you feel, what you regret, where the money is stashed.
            <% end %>


          </p>
        </div>
      </div>
    </div>
    <div class="tipUnit">
      <div class="tipUnit-inner">
        <%= image_tag "../assets/images/Calendar.png", :class => "pull-left" %>
        <div class="infoUnit" id="inforUnit2">

          <% if !@homepage_content.nil? %>
              <h3><%= raw @homepage_content.pagefoot_inforunit2_head %></h3>
          <% else %>
              <h3>Keep your love to familiers</h3>
          <% end %>


          <p>
            <% if !@homepage_content.nil? %>
                <%= raw @homepage_content.pagefoot_inforunit2_content %>
            <% else %>
                Let’s us keep watching everyday and send you noticifation over Email - Message - more and more ways in
                futured.
                You always know that familiers will have last wish when ever you stop say “now iam not alive!”
            <% end %>
          </p>
        </div>
      </div>
    </div>
    <div class="clearfix"></div>
    <div class="tipUnit">
      <div class="tipUnit-inner">
        <%= image_tag "../assets/images/Shield.png", :class => "pull-left" %>
        <div class="infoUnit" id="inforUnit3">

          <% if !@homepage_content.nil? %>
              <h3><%= raw @homepage_content.pagefoot_inforunit3_head %></h3>
          <% else %>
              <h3>Private data 100%</h3>
          <% end %>


          <p>
            <% if !@homepage_content.nil? %>
                <%= raw @homepage_content.pagefoot_inforunit3_content %>
            <% else %>
                We sure all your data save in our system is private with best security! Addition, you also have more and
                more functions to controll packages - items if upgrade to plane Premium Account.
            <% end %>


          </p>
        </div>
      </div>
    </div>
    <div class="tipUnit">
      <div class="tipUnit-inner">
        <%= image_tag "../assets/images/Camera.png", :class => "pull-left" %>
        <div class="infoUnit" id="inforUnit4">

          <% if !@homepage_content.nil? %>
              <h3><%= raw @homepage_content.pagefoot_inforunit4_head %></h3>
          <% else %>
              <h3>Rich media data</h3>
          <% end %>


          <p>
            <% if !@homepage_content.nil? %>
                <%= raw @homepage_content.pagefoot_inforunit4_content %>
            <% else %>
                From now, we provide 3 kinds of data : Textnote - Image - files (extension: zip, rar, 7zip and
                extensions
                not
                excute). We will try to take more kinds of data in future to you!
            <% end %>


          </p>
        </div>
      </div>
    </div>
  </div>
</div>